# Build artifacts — regenerated by `npm run build`.
/dist/
/node_modules/

# Bundled adapter source — copied FROM reference/react-oidc/ by scripts/bundle.sh at build time.
# Never edit here; never commit. The single source of truth is reference/react-oidc/.
/templates/
