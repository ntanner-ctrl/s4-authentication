# Installed deps (regenerated by `npm install`)
node_modules/

# Compiled schematic output (regenerated by `npm run build`; only the
# distribution branch — Plan 3 — ships the .js).
schematics/**/*.js
schematics/**/*.js.map
